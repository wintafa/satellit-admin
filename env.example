# MongoDB Connection String
# ⚠️ ВАЖНО: Для Docker используйте имя сервиса "mongo", а не 127.0.0.1!
# Для локального Docker используйте:
DATABASE_URL=mongodb://mongo:27017/printshop

# Для внешней MongoDB (например, MongoDB Atlas):
# DATABASE_URL=mongodb://username:password@host:27017/printshop?authSource=admin

# ⚠️ НЕ ИСПОЛЬЗУЙТЕ 127.0.0.1 в Docker! Это не будет работать!
# ❌ НЕПРАВИЛЬНО: DATABASE_URL=mongodb://127.0.0.1:27017/printshop

# Payload Secret - сгенерируйте случайную строку минимум 32 символа
# Можно сгенерировать командой: openssl rand -base64 32
PAYLOAD_SECRET=your-super-secret-key-here-minimum-32-characters-long

# Next.js порт (по умолчанию 3002)
PORT=3002

# Node Environment
NODE_ENV=production

# Опционально: MongoDB credentials (если используете аутентификацию)
# MONGO_USER=admin
# MONGO_PASS=your-password



# Added by Payload
PAYLOAD_SECRET=ed933e5fe3b0862fc2cf3faa
DATABASE_URL=mongodb://127.0.0.1/printshop
ADMIN_TOKEN="1"
DATABASE_URL=mongodb://127.0.0.1:27017/printshop
URL=vasha-tipografiya.ru

