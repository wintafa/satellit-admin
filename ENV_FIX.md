# 🔧 Исправление .env файла

## ❌ Проблемы в вашем текущем .env:

### 1. Неправильный DATABASE_URL

```env
# ❌ НЕПРАВИЛЬНО (ваш текущий):
DATABASE_URL=mongodb://127.0.0.1:27017/printshop

# ✅ ПРАВИЛЬНО:
DATABASE_URL=mongodb://mongo:27017/printshop
```

**Почему:** В Docker контейнере `127.0.0.1` указывает на сам контейнер приложения, а не на контейнер MongoDB. Нужно использовать имя сервиса `mongo` из `docker-compose.yml`.

### 2. PAYLOAD_SECRET слишком короткий

```env
# ❌ НЕПРАВИЛЬНО (ваш текущий - только 24 символа):
PAYLOAD_SECRET=ed933e5fe3b0862fc2cf3faa

# ✅ ПРАВИЛЬНО (минимум 32 символа):
PAYLOAD_SECRET=ed933e5fe3b0862fc2cf3faa1234567890abcdef
```

**Почему:** Payload CMS требует минимум 32 символа для `PAYLOAD_SECRET`.

## ✅ Правильный .env файл:

```env
# MongoDB Connection String
# Для локального Docker используйте имя сервиса "mongo":
DATABASE_URL=mongodb://mongo:27017/printshop

# Payload Secret - минимум 32 символа!
# Сгенерируйте новый: openssl rand -base64 32
PAYLOAD_SECRET=ed933e5fe3b0862fc2cf3faa1234567890abcdef1234567890abcdef

# Next.js порт
PORT=3002

# Node Environment
NODE_ENV=production
```

## 🔧 Как исправить на сервере:

1. **Сгенерируйте новый PAYLOAD_SECRET:**
   ```bash
   openssl rand -base64 32
   ```

2. **Отредактируйте .env файл:**
   ```bash
   nano .env
   ```

3. **Исправьте DATABASE_URL:**
   ```env
   DATABASE_URL=mongodb://mongo:27017/printshop
   ```

4. **Исправьте PAYLOAD_SECRET** (используйте сгенерированный выше)

5. **Перезапустите контейнеры:**
   ```bash
   docker-compose down
   docker-compose up -d
   ```

## 📝 Примечание:

В `docker-compose.yml` уже есть правильный `DATABASE_URL` в секции `environment`, но переменные из `.env` имеют приоритет. Поэтому важно исправить `.env` файл.

