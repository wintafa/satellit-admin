# 🚀 Полная пошаговая инструкция по развертыванию Printshop на сервере

Это подробная инструкция для развертывания проекта Printshop на сервере с нуля до полностью рабочего состояния.

---

## 📋 Шаг 1: Подготовка сервера

### 1.1. Подключение к серверу

```bash
# Подключитесь к серверу по SSH
ssh user@your-server-ip

# Или если используете ключ
ssh -i ~/.ssh/your-key.pem user@your-server-ip
```

### 1.2. Обновление системы

```bash
# Обновление списка пакетов
sudo apt-get update

# Обновление установленных пакетов
sudo apt-get upgrade -y
```

### 1.3. Установка Docker

```bash
# Установка необходимых пакетов
sudo apt-get install -y \
    ca-certificates \
    curl \
    gnupg \
    lsb-release

# Добавление официального GPG ключа Docker
sudo mkdir -p /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg

# Настройка репозитория
echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu \
  $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

# Установка Docker
sudo apt-get update
sudo apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

# Проверка установки
docker --version
docker compose version
```

**Или быстрая установка через официальный скрипт:**

```bash
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Добавление текущего пользователя в группу docker (чтобы не использовать sudo)
sudo usermod -aG docker $USER

# Выйдите и войдите снова, чтобы изменения вступили в силу
exit
# Затем подключитесь снова
```

### 1.4. Установка Nginx

```bash
sudo apt-get install -y nginx

# Проверка статуса
sudo systemctl status nginx

# Запуск и автозапуск
sudo systemctl start nginx
sudo systemctl enable nginx
```

### 1.5. Настройка файрвола (UFW)

```bash
# Разрешить SSH (важно сделать ПЕРВЫМ!)
sudo ufw allow 22/tcp

# Разрешить HTTP и HTTPS
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# Включить файрвол
sudo ufw enable

# Проверка статуса
sudo ufw status
```

---

## 📦 Шаг 2: Загрузка проекта на сервер

### 2.1. Создание директории для проекта

```bash
# Создайте директорию (например, в /var/www или /opt)
sudo mkdir -p /var/www/printshop
sudo chown $USER:$USER /var/www/printshop
cd /var/www/printshop
```

### 2.2. Загрузка файлов проекта

**Вариант A: Через Git (рекомендуется)**

```bash
# Если проект в Git репозитории
git clone <your-repo-url> /var/www/printshop
cd /var/www/printshop
```

**Вариант B: Через SCP (с локальной машины)**

```bash
# На ЛОКАЛЬНОЙ машине выполните:
cd C:\Users\ymka6\Documents\vscode\Printshop

# Загрузите все файлы проекта (кроме node_modules и .next)
scp -r \
  --exclude='node_modules' \
  --exclude='.next' \
  --exclude='.git' \
  ./ user@your-server-ip:/var/www/printshop/
```

**Вариант C: Через SFTP (FileZilla, WinSCP и т.д.)**

1. Подключитесь к серверу через SFTP
2. Загрузите все файлы проекта в `/var/www/printshop`
3. **НЕ загружайте:** `node_modules`, `.next`, `.git`

### 2.3. Проверка загруженных файлов

```bash
cd /var/www/printshop
ls -la

# Должны быть файлы:
# - package.json
# - docker-compose.yml
# - Dockerfile
# - next.config.ts
# - src/
# - public/
# - mongo-backup/ (если есть)
```

---

## ⚙️ Шаг 3: Настройка переменных окружения

### 3.1. Создание .env файла

```bash
cd /var/www/printshop

# Создайте .env файл из примера
cp env.example .env

# Откройте для редактирования
nano .env
```

### 3.2. Заполнение .env файла

```env
# MongoDB Connection String
# ВАЖНО: Для Docker используйте имя сервиса "mongo"
DATABASE_URL=mongodb://mongo:27017/printshop

# Payload Secret - сгенерируйте безопасный ключ
# Выполните на сервере: openssl rand -base64 32
PAYLOAD_SECRET=ваш-сгенерированный-секрет-минимум-32-символа

# Next.js порт
PORT=3002

# Node Environment
NODE_ENV=production
```

**Генерация PAYLOAD_SECRET:**

```bash
# Сгенерируйте безопасный ключ
openssl rand -base64 32

# Скопируйте результат и вставьте в .env файл вместо PAYLOAD_SECRET
```

**Пример заполненного .env:**

```env
DATABASE_URL=mongodb://mongo:27017/printshop
PAYLOAD_SECRET=K8j3mN9pQ2rT5vX8zA1bC4eF7hI0kL3nO6qR9sU2wY5zA8bC1dE4fG7hI0jK3
PORT=3002
NODE_ENV=production
```

**Сохраните файл:** `Ctrl+O`, `Enter`, `Ctrl+X`

### 3.3. Проверка .env файла

```bash
# Проверьте, что файл создан и заполнен
cat .env

# Убедитесь, что нет лишних пробелов и все значения заполнены
```

---

## 💾 Шаг 4: Восстановление базы данных из бэкапа

### 4.1. Проверка наличия бэкапа

```bash
cd /var/www/printshop

# Проверьте структуру бэкапа
ls -la mongo-backup/

# Должна быть папка printshop с файлами .bson и .metadata.json
ls -la mongo-backup/printshop/
```

### 4.2. Запуск MongoDB (если еще не запущена)

```bash
# Сначала запустим только MongoDB для восстановления
docker compose up -d mongo

# Подождите 10-15 секунд, пока MongoDB запустится
sleep 15

# Проверьте статус
docker compose ps
```

### 4.3. Восстановление базы данных

**Вариант A: Использование скрипта (рекомендуется)**

```bash
# Сделайте скрипт исполняемым
chmod +x scripts/restore-mongo-direct.sh

# Запустите восстановление
./scripts/restore-mongo-direct.sh

# Скрипт автоматически проверит наличие бэкапа и запустит MongoDB если нужно
```

**Вариант B: Вручную**

```bash
# Восстановление из бэкапа
docker exec -i printshop_mongo mongorestore \
    --db printshop \
    --drop \
    /backup/printshop

# Должно появиться сообщение об успешном восстановлении
# "X document(s) restored successfully. 0 document(s) failed to restore."
```

**Проверка восстановления:**

```bash
# Подключитесь к MongoDB и проверьте коллекции
docker exec -it printshop_mongo mongosh printshop

# В MongoDB shell выполните:
show collections

# Должны быть коллекции:
# - users
# - media
# - globals
# - payload-preferences
# - payload-kvs
# - payload-migrations
# - payload-locked-documents

# Выйдите из MongoDB shell
exit
```

---

## 🔨 Шаг 5: Сборка и запуск приложения

### 5.1. Настройка прав доступа для скриптов

```bash
cd /var/www/printshop

# Сделайте скрипты исполняемыми
chmod +x scripts/*.sh
```

### 5.2. Сборка и запуск через Docker Compose

```bash
# Остановите MongoDB, если она запущена отдельно
docker compose down

# Соберите и запустите все сервисы
docker compose up -d --build

# Это займет несколько минут при первой сборке
# Следите за процессом
docker compose logs -f
```

**Или используйте скрипт развертывания:**

```bash
./scripts/deploy.sh
```

### 5.3. Проверка статуса контейнеров

```bash
# Проверьте статус всех контейнеров
docker compose ps

# Оба контейнера должны быть в статусе "Up" и "healthy"
# - printshop_app (Up, healthy)
# - printshop_mongo (Up, healthy)
```

### 5.4. Проверка логов

```bash
# Логи приложения
docker compose logs printshop_app

# Логи MongoDB
docker compose logs mongo

# Логи в реальном времени
docker compose logs -f printshop_app
```

### 5.5. Проверка доступности приложения

```bash
# Проверка health endpoint
curl http://localhost:3002/api/health

# Должен вернуть:
# {"status":"ok","timestamp":"2024-..."}

# Проверка главной страницы
curl http://localhost:3002

# Должен вернуть HTML код страницы
```

---

## 🌐 Шаг 6: Настройка Nginx как Reverse Proxy

### 6.1. Редактирование конфигурации Nginx

```bash
cd /var/www/printshop

# Откройте nginx.conf
nano nginx.conf
```

### 6.2. Обновление домена в конфигурации

Найдите строку:
```nginx
server_name your-domain.com www.your-domain.com;
```

Замените на ваш реальный домен:
```nginx
server_name example.com www.example.com;
```

**Сохраните:** `Ctrl+O`, `Enter`, `Ctrl+X`

### 6.3. Копирование конфигурации в Nginx

```bash
# Скопируйте конфигурацию
sudo cp nginx.conf /etc/nginx/sites-available/printshop

# Создайте символическую ссылку
sudo ln -s /etc/nginx/sites-available/printshop /etc/nginx/sites-enabled/

# Удалите дефолтную конфигурацию (опционально)
sudo rm /etc/nginx/sites-enabled/default
```

### 6.4. Проверка и перезапуск Nginx

```bash
# Проверка синтаксиса конфигурации
sudo nginx -t

# Если все ОК, перезапустите Nginx
sudo systemctl restart nginx

# Проверка статуса
sudo systemctl status nginx
```

### 6.5. Проверка работы через Nginx

```bash
# Проверка через HTTP (пока без SSL)
curl http://localhost

# Или откройте в браузере: http://your-domain.com
```

---

## 🔒 Шаг 7: Настройка SSL сертификата (HTTPS)

### 7.1. Установка Certbot

```bash
# Установка Certbot
sudo apt-get install -y certbot python3-certbot-nginx
```

### 7.2. Получение SSL сертификата

```bash
# Замените example.com на ваш домен
sudo certbot --nginx -d example.com -d www.example.com

# Certbot задаст несколько вопросов:
# - Email для уведомлений (введите ваш email)
# - Согласие с условиями (A)
# - Редирект HTTP на HTTPS (2 - редирект)
```

### 7.3. Автоматическое обновление сертификата

Certbot автоматически настроит cron для обновления сертификата. Проверьте:

```bash
# Проверка автоматического обновления
sudo certbot renew --dry-run
```

### 7.4. Обновление конфигурации Nginx для HTTPS

После получения сертификата Certbot автоматически обновит конфигурацию. Если нужно вручную:

```bash
# Откройте конфигурацию
sudo nano /etc/nginx/sites-available/printshop

# Раскомментируйте блок server для порта 443
# Обновите пути к сертификатам (Certbot сделает это автоматически)
```

### 7.5. Проверка HTTPS

```bash
# Проверка через curl
curl https://your-domain.com

# Или откройте в браузере: https://your-domain.com
```

---

## ✅ Шаг 8: Финальная проверка

### 8.1. Чеклист проверки

```bash
# 1. Проверка контейнеров
docker compose ps
# ✅ Оба контейнера должны быть "Up" и "healthy"

# 2. Проверка приложения
curl http://localhost:3002/api/health
# ✅ Должен вернуть {"status":"ok",...}

# 3. Проверка Nginx
sudo nginx -t
# ✅ Должно быть "syntax is ok"

# 4. Проверка доступности через домен
curl https://your-domain.com
# ✅ Должен вернуть HTML код

# 5. Проверка админ-панели
curl https://your-domain.com/admin
# ✅ Должен вернуть HTML код админ-панели
```

### 8.2. Проверка в браузере

1. Откройте `https://your-domain.com` - должна открыться главная страница
2. Откройте `https://your-domain.com/admin` - должна открыться админ-панель Payload CMS
3. Попробуйте войти в админ-панель (используйте данные из базы)

### 8.3. Проверка базы данных

```bash
# Подключитесь к MongoDB
docker exec -it printshop_mongo mongosh printshop

# Проверьте количество документов
db.users.countDocuments()
db.media.countDocuments()
db.globals.countDocuments()

# Выйдите
exit
```

---

## 🔄 Шаг 9: Настройка автоматических бэкапов

### 9.1. Создание задачи cron для бэкапов

```bash
# Откройте crontab
crontab -e

# Добавьте строку для ежедневного бэкапа в 2:00 ночи
0 2 * * * cd /var/www/printshop && ./scripts/backup-mongo.sh >> /var/log/printshop-backup.log 2>&1

# Сохраните и выйдите
```

### 9.2. Проверка бэкапа вручную

```bash
cd /var/www/printshop

# Создайте бэкап вручную
./scripts/backup-mongo.sh

# Проверьте, что бэкап создан
ls -la mongo-backup/
```

---

## 📊 Шаг 10: Мониторинг и обслуживание

### 10.1. Полезные команды для мониторинга

```bash
# Статус контейнеров
docker compose ps

# Использование ресурсов
docker stats

# Логи приложения
docker compose logs -f printshop_app

# Логи MongoDB
docker compose logs -f mongo

# Размер дискового пространства
df -h

# Использование памяти
free -h
```

### 10.2. Обновление приложения

```bash
cd /var/www/printshop

# 1. Создайте бэкап базы данных
./scripts/backup-mongo.sh

# 2. Обновите код (если используете Git)
git pull

# 3. Остановите контейнеры
docker compose down

# 4. Пересоберите образы
docker compose build --no-cache printshop

# 5. Запустите контейнеры
docker compose up -d

# 6. Проверьте логи
docker compose logs -f printshop_app
```

---

## 🛠️ Устранение проблем

### Проблема: Контейнеры не запускаются

```bash
# Проверьте логи
docker compose logs

# Проверьте .env файл
cat .env

# Проверьте, что порты свободны
sudo netstat -tulpn | grep 3002
sudo netstat -tulpn | grep 27017
```

### Проблема: MongoDB не подключается

```bash
# Проверьте статус MongoDB
docker compose ps mongo

# Проверьте логи MongoDB
docker compose logs mongo

# Проверьте DATABASE_URL в .env
cat .env | grep DATABASE_URL

# Должно быть: DATABASE_URL=mongodb://mongo:27017/printshop
```

### Проблема: Nginx не работает

```bash
# Проверьте конфигурацию
sudo nginx -t

# Проверьте логи
sudo tail -f /var/log/nginx/error.log

# Проверьте, что приложение доступно
curl http://localhost:3002
```

### Проблема: SSL сертификат не работает

```bash
# Проверьте сертификат
sudo certbot certificates

# Обновите сертификат вручную
sudo certbot renew

# Проверьте конфигурацию Nginx
sudo nginx -t
```

---

## 📝 Итоговый чеклист

- [ ] Docker и Docker Compose установлены
- [ ] Nginx установлен и настроен
- [ ] Файрвол настроен (UFW)
- [ ] Проект загружен на сервер
- [ ] .env файл создан и заполнен
- [ ] PAYLOAD_SECRET сгенерирован (32+ символов)
- [ ] База данных восстановлена из бэкапа
- [ ] Контейнеры запущены и работают
- [ ] Приложение доступно на localhost:3002
- [ ] Nginx настроен как reverse proxy
- [ ] Домен настроен и указывает на сервер
- [ ] SSL сертификат установлен
- [ ] HTTPS работает
- [ ] Админ-панель доступна
- [ ] Автоматические бэкапы настроены

---

## 🎉 Готово!

Ваш проект Printshop развернут и работает на сервере!

**Доступные адреса:**
- Главная страница: `https://your-domain.com`
- Админ-панель: `https://your-domain.com/admin`

**Полезные команды:**
```bash
# Остановка
docker compose down

# Запуск
docker compose up -d

# Перезапуск
docker compose restart

# Логи
docker compose logs -f

# Бэкап
./scripts/backup-mongo.sh
```

---

## 📞 Дополнительная помощь

Если возникли проблемы:
1. Проверьте логи: `docker compose logs`
2. Проверьте документацию в `DEPLOY.md` и `TROUBLESHOOTING.md`
3. Проверьте конфигурацию в `.env` файле
4. Убедитесь, что все шаги выполнены правильно

